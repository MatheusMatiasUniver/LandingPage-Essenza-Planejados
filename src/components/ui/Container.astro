---
interface Props {
  class?: string;
  as?: 'div' | 'section' | 'article' | 'main';
  id?: string;
}

const {
  class: className = '',
  as: Tag = 'div',
  id,
} = Astro.props;
---

<Tag
  class:list={['max-w-7xl mx-auto px-5 sm:px-6 lg:px-8', className]}
  id={id}
>
  <slot />
</Tag>
